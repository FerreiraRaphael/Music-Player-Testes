import historicReducer from './historic'

describe('Historic reducer spec', () => {
  it('Should be a function',() => {
    expect(historicReducer).to.be.a('function')
  })
  // it('Should return a list')
})
